import { TryAPI } from '@/components/try-api';

# Validator Voting Power Events

## **[GET]** /v1/:network/validators/:validatorAddress/proposed

Get blocks proposed by the validator

### Request

```yaml filename="Headers"
Authorization: Bearer {access_token}
```

```yaml filename="Parameters"
# The name of network
# (string, required)
network: cosmos
# The address of account
# (string, required)
validatorAddress: cosmosvaloper1clpqr4nrk4khgkxj78fcwwh6dl3uw4epsluffn
```

```yaml filename="Queries"
# Reqeust per page
# (number, optional, default: 20)
take: 20
# Value of `pagination.searchAfter` from previous request for pagination
# (string, optional)
searchAfter: MTY4OTc1NTU4NjAwMA==
```

<TryAPI
  method="GET"
  host="https://apis.mintscan.io"
  url="/v1/:network/validators/:validatorAddress/proposed"
  query={[
    { key: 'take', type: 'number', optional: true, placeholder: '20' },
    { key: 'searchAfter', type: 'string', optional: true, placeholder: 'MTY4OTc1NTU4NjAwMA==' },
  ]}
  useBearerAuthorization
/>
