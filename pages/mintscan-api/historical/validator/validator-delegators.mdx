# Validator Delegators

## **[GET]** /v1/:network/validators/:validatorAddress/delegators

Get a list of richlist of a symbol

### Request

```yaml filename="Headers"
Authorization: Bearer {access_token}
```

```yaml filename="Parameters"
# The name of network
# (string, required)
network: cosmos
# The address of account
# (string, required)
validatorAddress: cosmosvaloper1clpqr4nrk4khgkxj78fcwwh6dl3uw4epsluffn
```

```yaml filename="Queries"
# Reqeust per page
# (number, optional, default: 20)
take: 10
# Value of skip amount for pagination
# (number, optional, default: 0)
from: 20
```

import { TryAPI } from '@/components/try-api';

<TryAPI
  method="GET"
  host="http://148.113.165.91"
  url="/v1/:network/validators/:validatorAddress/delegators"
  query={[
    { key: 'take', type: 'number', optional: true, placeholder: '20' },
    { key: 'from', type: 'number', optional: true, placeholder: '0' },
  ]}
  useBearerAuthorization
/>
